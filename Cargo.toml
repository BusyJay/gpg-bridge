[package]
name = "gnupg-bridge"
version = "0.1.0"
authors = ["Jay Lee <busyjaylee@gmail.com>"]
edition = "2018"

[dependencies]
log = "0.4"
tokio = { version = "0.2", features = ["rt-core", "tcp", "io-util", "macros", "dns"] }
